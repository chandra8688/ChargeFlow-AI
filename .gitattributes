# Git Large File Storage (LFS) configuration for ChargeFlow AI V2
# Track heavy model weight binaries with Git LFS (demand_forecaster.joblib ~102.4 MB)
artifacts/models/*.joblib filter=lfs diff=lfs merge=lfs -text
